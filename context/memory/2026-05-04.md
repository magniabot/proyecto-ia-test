# 2026-05-04

## Dashboard Global — fix de agendamientos reactivados

Detectado: dashboard global-2026-04-30 mostraba solo 2 agendamientos en abril, pero CRM tenía más (de leads reactivados/antiguos). Causa: `export-crm.js` filtraba opportunities por `createdAt`, perdiendo opps de leads viejos movidos a "Agendado" en el período.

### Cambios

- `.claude/skills/ghl-exporter/scripts/export-crm.js`: opportunities ahora se filtran por `updatedAt` (último cambio) en vez de `createdAt`. Esto captura reactivaciones.
- `.claude/skills/dashboard-global/scripts/build.py`:
  - Acepta `--date=YYYY-MM-DD` para override del filename de salida.
  - Anchora período al `date_start`/`date_stop` del CSV de Meta cuando existe.
  - Filtra contactos por dateAdded en período (mantiene "leads del período").
  - Filtra opps por updatedAt en período (incluye reactivaciones).
  - Lista de agendados con badge "Nuevo"/"Reactivado", muestra fecha de creación del lead y fecha de agendamiento.
  - KPI principal "Agendamientos" muestra desglose: X nuevos · Y reactivados.
  - Nota explicativa en tabla "Performance por canal" (atribución por canal solo cuenta nuevos del período).

### Resultado

Re-fetcheado con `node export-crm.js --days=35 --skip-messages` para alinear con período Meta (2026-03-30 → 2026-04-28). Regenerado `created/dashboards/global-2026-04-30.html`.

- Antes: 2 agendamientos · CP-Agend $640k
- Ahora: **7 agendamientos** (2 nuevos + 5 reactivados) · CP-Agend $182,737

Reactivados detectados: Pedro Arnés (mar→abr), Marisel Tirado (mar→abr), Patricio Pizarro (ene→abr), Manuel Castillo (dic 2025→abr), Adolfo Espinoza (jul 2025→abr).

### Nota técnica

El proxy "updatedAt" no es 100% preciso para detectar el momento exacto de cambio a stage Agendado (cualquier edición actualiza el timestamp). Para mayor precisión a futuro, considerar usar el endpoint `/calendars/events` o el stage history de GHL.
